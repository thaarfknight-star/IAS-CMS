# تاریخچه نسخه‌ها — IAS-CMS

## 2.0.36-beta (۱۴۰۵/۰۷/۰۱)
- **نصب‌کننده‌ی استاندارد ویندوز:** صفحه‌ی نصب سفارشی حذف شد؛ حالا ویزارد
  استاندارد ویندوز است: خوش‌آمد → انتخاب پوشه → نصب → پایان (فارسی،
  راست‌به‌چپ) با تیک «اجرای برنامه» در پایان. بررسی «برنامه باز است؟»،
  میان‌برها، رجیستری و uninstall.exe بدون تغییر حفظ شدند. حذف‌کننده با
  دکمه‌ی قرمز «حذف کامل» دست‌نخورده باقی ماند.

## 2.0.35-beta — نسخه‌ی پایداری تحویل (۱۴۰۵/۰۷/۰۱)
- **محافظ دیتابیس:** حالت WAL برای همه‌ی دیتابیس‌ها (اشخاص، پلاک، گزارش‌ها)؛
  خواندن/نوشتن هم‌زمان تردها دیگر دیتابیس را قفل نمی‌کند.
- **بکاپ خودکار روزانه:** در هر اجرا فقط یک‌بار بکاپ گرفته می‌شود؛
  ۷ بکاپ آخر در پوشه‌ی `backups` نگه داشته می‌شود.
- **نگهبان حافظه:** ترد هر دوربین هر ۶۰۰ فریم حافظه‌ی بلااستفاده را آزاد می‌کند؛
  مناسب سیستم‌های ۴ گیگابایت رم در پخش طولانی‌مدت.
- موارد قبلی همچنان پابرجاست: تور امنیت سراسری (ثبت کرش بدون بستن برنامه)،
  اتصال مجدد خودکار RTSP، کول‌داون ۱۵ ثانیه‌ای پلاک.

## 2.0.34-beta
- ثبت پلاک و شخصِ در حال حرکت (ترک آگاه از حرکت، تأیید با ۲ خوانش)؛
  زمان‌بندی متناوب مدل‌های سنگین برای سبک‌تر شدن هر تیک.

## 2.0.33-beta
- حذف مسیرهای پیش‌فرض، ویرایش مسیر، دابل‌کلیک = حذف نقطه،
  حذف شبیه‌سازی مسیر، ترمیم رفرش پوشش زنده.
