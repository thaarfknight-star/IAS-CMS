# تاریخچه نسخه‌ها — IAS-CMS

## 2.0.48-beta (۱۴۰۵/۰۷/۰۴)
- **غیرفعال‌سازی سهمیه‌های تعدادی:** هر سهمیه‌ی تعدادی (کل دوربین‌ها،
  پلاک‌خوان، حریق، ردیابی اشخاص) حالا در فایل لایسنس یک پرچم فعال/غیرفعال
  هم دارد (`quota_enabled`)؛ سهمیه‌ی غیرفعال یعنی «صفر واقعی» — فعال‌سازی
  آن قابلیت کاملاً بسته می‌شود. عدد ۰ همچنان یعنی نامحدود.
- دیالوگ مدیریت لایسنس سهمیه‌های غیرفعال را با «❌ غیرفعال» نشان می‌دهد.
- پیام‌ها: «قابلیت غیرفعال» به‌جای «سهمیه تکمیل است» برای این حالت.
- سازگاری عقب‌رو: لایسنس‌های قدیمی بدون `quota_enabled` (مثل لایسنس
  توسعه‌دهنده) همه‌ی سهمیه‌ها را فعال در نظر می‌گیرند.

## 2.0.47-beta (۱۴۰۵/۰۷/۰۴)
- **رفع کرش استارتاپ:** تایپی `adhint` به‌جای `admhint` در گروه ادمین صفحه‌ی
  تنظیمات باعث `NameError` و بسته‌شدن برنامه موقع بالا آمدن می‌شد؛ اصلاح شد.

## 2.0.46-beta (۱۴۰۵/۰۷/۰۴)
- **اصلاح مدل لایسنس (به دستور کاربر):** دو دسته کنترل در لایسنس —
  «قابلیت‌های روشن/خاموش» بدون محدودیت تعداد دوربین (شمارش افراد،
  چهره‌خوان، هشدار ورود به محدوده) و «سهمیه‌های تعدادی» (کل دوربین‌ها،
  پلاک‌خوان، حریق، ردیابی اشخاص).
- **حالت محدود جدید (بدون لایسنس معتبر):** تصویر همه‌ی دوربین‌ها نمایش
  داده می‌شود؛ فقط شمارش افراد فعال است؛ چهره‌خوان و هشدار ورود به محدوده
  کار نمی‌کنند و هیچ قابلیت سهمیه‌ای قابل فعال‌سازی نیست.
- دیالوگ مدیریت لایسنس حالا وضعیت قابلیت‌ها را هم نشان می‌دهد؛ برنامه‌ی
  «مدیریت لایسنس IAS» برای هر سه قابلیت چک‌باکس فعال/غیرفعال دارد.

## 2.0.45-beta (۱۴۰۵/۰۷/۰۴)
- **سیستم لایسنس کامل (به دستور کاربر):** برنامه حالا با فایل لایسنس
  امضاشده (`.lic`) کار می‌کند. امضا با Ed25519 است (پیاده‌سازی خالص
  پایتون، بدون وابستگی جدید) و کلید عمومی در برنامه جاسازی شده؛
  دست‌کاری فایل لایسنس امضا را باطل می‌کند. لایسنس شامل نام مشتری، تاریخ
  انقضا، قفل سخت‌افزاری (اختیاری) و سهمیه‌ی قابلیت‌هاست (تعداد کل
  دوربین‌ها، پلاک‌خوان، تشخیص حریق، ردیابی اشخاص؛ ۰ یعنی نامحدود).
- **نرم‌افزار جدای «مدیریت لایسنس IAS»:** صدور لایسنس با یک برنامه‌ی
  کاملاً جدا انجام می‌شود (ریپوی `IAS-LicenseManager`) که با کلید خصوصی
  فروشنده فایل `.lic` می‌سازد؛ در برنامه‌ی اصلی فقط «وارد کردن» لایسنس
  ممکن است.
- **ورود لایسنس از صفحه‌ی تنظیمات:** دکمه‌ی «ورود ادمین» حالا دیالوگ
  مدیریت لایسنس را باز می‌کند: نمایش وضعیت/مشتری/انقضا/سهمیه‌ها، دکمه‌ی
  «کپی شناسه‌ی سخت‌افزاری» و «بارگذاری فایل لایسنس». تعیین سهمیه دیگر
  داخل برنامه‌ی اصلی ممکن نیست.
- **حالت محدود بدون لایسنس معتبر:** اگر فایل لایسنس نباشد/نامعتبر/منقضی
  باشد، برنامه با حداکثر ۱ دوربین و بدون قابلیت‌های هوشمند بالا می‌آید و
  یک‌بار هشدار می‌دهد؛ عنوان پنجره هم وضعیت لایسنس را نشان می‌دهد.
- سهمیه‌های دستی نسخه‌ی 2.0.44 (کلید `feature_quotas` در app_settings)
  دیگر خوانده نمی‌شوند؛ سقف‌ها فقط از لایسنس می‌آیند.

## 2.0.44-beta (۱۴۰۵/۰۷/۰۴)
- **محیط ادمین + سهمیه‌ی قابلیت‌ها (به دستور کاربر):** بخش «👑 محیط ادمین»
  به صفحه‌ی تنظیمات اضافه شد که فقط با رمز ادمین باز می‌شود (رمز به‌صورت
  هش SHA-256 نگه داشته می‌شود، نه متن ساده). در محیط جداگانه‌ی ادمین
  می‌توان سقف هر قابلیت را تعیین کرد: تعداد کل دوربین‌ها، دوربین
  پلاک‌خوان، دوربین تشخیص حریق و دوربین ردیابی اشخاص (۰ یعنی نامحدود).
  وقتی سهمیه‌ای پر شود، فعال‌سازی جدید آن قابلیت روی دوربین دیگر (از
  صفحه‌ی پلاک‌خوان/اعلام حریق/ردیابی اشخاص یا افزودن دوربین و کانال NVR)
  با پیام «سهمیه تکمیل است» مسدود می‌شود؛ دوربین‌هایی که قبلاً فعال‌اند
  سر جایشان می‌مانند.

## 2.0.43-beta (۱۴۰۵/۰۷/۰۴)
- **رفع خطای «موتور = شخص» در شمارش افراد:** وقتی مدل YOLOv8n خودش یک
  موتور/خودروی پارک‌شده را مستقیماً «شخص» تشخیص می‌داد، هیچ باکس وسیله‌ای
  برای فیلتر IoU وجود نداشت و موتور شمرده می‌شد. حالا باکس‌های واضحاً
  عریض‌تر از قد (مثل موتور از بغل) فقط وقتی «شخص» حساب می‌شوند که یک چهره
  داخلشان باشد (با کمک نتایج تشخیص چهره)؛ فرد نشسته‌ی روبه‌دوربین نگه
  داشته می‌شود و موتور پارک‌شده حذف می‌شود. کلاس‌های وسیله‌ی نقلیه هم از
  [دوچرخه، موتور] به همه‌ی وسایل COCO (خودرو، اتوبوس، کامیون) گسترش یافت.

## 2.0.42-beta (۱۴۰۵/۰۷/۰۱)
- **نصب‌کننده فقط فارسی شد (به دستور کاربر):** دیالوگ انتخاب زبان و همه‌ی
  رشته‌های انگلیسی از installer.nsi حذف شد؛ نصب‌کننده فقط با زبان Farsi
  بالا می‌آید.
- **برگرداندن چیدمان پنل‌ها:** در 2.0.39 فراخوانی `i18n.tr_app` جهت کل برنامه
  را به راست‌به‌چپ تغییر داده بود و جای پنل‌ها آینه‌ای شده بود؛ با حذف سیستم
  دوزبانه، چیدمان دقیقاً به حالت 2.0.38 برگشت (تأیید شد: main.py و فایل‌های
  پلاک‌خوان بایت‌به‌بایت با 2.0.38 یکسان‌اند).

## 2.0.41-beta (۱۴۰۵/۰۷/۰۱)
- **حذف کامل سیستم دوزبانه (به دستور کاربر):** رابط برنامه به فارسی خالص
  برگشت؛ فایل `i18n.py` و همه‌ی رشته‌های انگلیسی از main.py، صفحه‌ی تنظیمات
  و هر سه فایل پلاک‌خوان حذف شد. گروه انتخاب زبان از صفحه‌ی تنظیمات برداشته
  شد و توابع `get_language`/`set_language` از app_settings حذف شدند.
- رفع باگ تعریف پلاک از «گزارش عبور» (رفرش جدول) که در 2.0.39 آمده بود حفظ شد.

## 2.0.40-beta (۱۴۰۵/۰۷/۰۱)
- **رفع کرش استارتاپ:** برنامه روی ویندوز موقع بالا آمدن با خطای
  `'SettingsPage' object has no attribute '_lang'` کرش می‌کرد؛ در بازنویسی
  دوزبانه‌ی صفحه‌ی تنظیمات (2.0.39-beta) مقداردهی `self._lang` حذف شده بود
  ولی سازنده‌ی گروه زبان هنوز به آن ارجاع می‌داد. حالا کمبوباکس زبان مستقیم
  از `app_settings.get_language()` می‌خواند.

## 2.0.39-beta (۱۴۰۵/۰۷/۰۱)
- **رفع باگ:** پلاکی که از «گزارش عبور» (جزئیات عبور) تعریف می‌شد، حالا فوراً
  در جدول تب «تعریف پلاک‌ها» ظاهر می‌شود (رفرش جدول بعد از ثبت).
- **رابط اصلی دوزبانه شد (فارسی RTL / English LTR):** زیرساخت مشترک `i18n.py`؛
  ۸ دکمه‌ی هدر، عنوان پنجره و جهت کل برنامه؛ صفحه‌ی تنظیمات؛ کل صفحه‌ی
  پلاک‌خوان (فرم تعریف/ویرایش، جزئیات عبور، وضعیت زنده، و هر ۵ تب: تعریف
  پلاک‌ها، گزارش عبور، مسیرها و قوانین، تخلفات تردد، لیست تحت‌نظر)؛ داشبورد
  آماری تردد؛ برچسب‌های نوع/رنگ خودرو، نوع تخلف، لیست تحت‌نظر و عبور/ورود-خروج.
  زبان از تنظیمات خوانده می‌شود و با راه‌اندازی مجدد کامل اعمال می‌شود.
- **متن جزئیات تخلف** در لحظه‌ی ثبت به زبان فعلی نوشته می‌شود (رکوردهای قدیمی
  فارسی می‌مانند)؛ در حالت English، کمبوباکس‌های نوع/رنگ خودرو انگلیسی نشان
  می‌دهند ولی مقدار فارسی در دیتابیس ذخیره می‌شود (چیدمان فیزیکی پلاک ایرانی
  و موتورسیکلت بدون تغییر).

## 2.0.38-beta (۱۴۰۵/۰۷/۰۱)
- **نصب‌کننده دوزبانه:** اول نصب، زبان انتخاب می‌شود (فارسی/English)؛ همه‌ی
  صفحه‌های استاندارد و پیام‌های سفارشی (اجرای برنامه، «برنامه باز است؟»،
  تایید حذف) به هر دو زبان‌اند. انتخاب زبان ذخیره می‌شود.
- **لوگوی برنامه در نصب‌کننده:** صفحه‌ی خوش‌آمد و هدر صفحه‌ها حالا لوگوی
  «ایمن آرا سورنا» را نشان می‌دهند (به‌جای تصویر پیش‌فرض NSIS)؛ آیکن فایل
  نصب هم همان لوگوی برنامه است.
- **پوشش خاموش/روشن‌شونده:** دکمه‌ی «📡 پوشش» حالا تاگل است؛ با زدنِ دوباره،
  تحلیل پوشش متوقف و نقاط از روی نقشه پاک می‌شوند.
- **رفع کندی نقشه:** پاک‌سازی نقاط پوشش دیگر همه‌ی آیتم‌های همه‌ی طبقه‌ها را
  نمی‌گردد (فقط طبقه‌ی جاری) و رفرشِ زنده گارد بازگشتی دارد؛ درگ دوربین روی
  نقشه‌های بزرگ روان‌تر شد.
- **رفع «دکمه‌های تایید/لغو رسم ظاهر نمی‌شن»:** حین رسمِ نقطه‌به‌نقطه، نوار
  ابزار زنده به‌روز می‌شود و دکمه‌ی «❌ لغو رسم» از همان اولین نقطه دیده و
  فعال است (لغوِ رسمِ نیمه‌کاره)؛ «✅ تایید و نام‌گذاری» بعد از بسته‌شدن
  چندضلعی می‌آید و همان‌جا اسم محدوده پرسیده می‌شود.

## 2.0.37-beta (۱۴۰۵/۰۷/۰۱)
- **نقاط رسم‌شده در حالت پوشش:** حالا همه‌ی نقطه‌هایی که برای رسم مسیر
  گذاشتی در حالت «📡 پوشش» نمایش داده می‌شوند — دایره‌ی بزرگ با حاشیه‌ی
  سفید و شماره‌ی نقطه؛ سبز = در دید دوربین، قرمز = خارج از دید. با نگه
  داشتن موس روی هر نقطه، اسم دوربین‌هایی که آن را می‌بینند هم دیده می‌شود.
  گزارش «پوشش زنده» هم خلاصه‌ی نقاط رسم‌شده را دارد.
- **پوشش زنده حین درگ:** دیگر لازم نیست دوربین را رها کنی تا پوشش به‌روز
  شود؛ حین جابه‌جا کردن دوربین، نقاط مسیر در لحظه سبز/قرمز می‌شوند
  (بدون ذخیره‌ی میانی در دیتابیس؛ ذخیره موقع رها کردن مثل قبل).
- **رفع خطای بیلد نصب‌کننده:** نام زبان فارسی در NSIS «Farsi» است نه
  «Persian» (فایل Persian.nlf اصلاً وجود ندارد)؛ بیلد Setup دوباره سبز می‌شود.

## 2.0.36-beta (۱۴۰۵/۰۷/۰۱)
- **نصب‌کننده‌ی استاندارد ویندوز:** صفحه‌ی نصب سفارشی حذف شد؛ حالا ویزارد
  استاندارد ویندوز است: خوش‌آمد → انتخاب پوشه → نصب → پایان (فارسی،
  راست‌به‌چپ) با تیک «اجرای برنامه» در پایان. بررسی «برنامه باز است؟»،
  میان‌برها، رجیستری و uninstall.exe بدون تغییر حفظ شدند. حذف‌کننده با
  دکمه‌ی قرمز «حذف کامل» دست‌نخورده باقی ماند.

## 2.0.35-beta — نسخه‌ی پایداری تحویل (۱۴۰۵/۰۷/۰۱)
- **محافظ دیتابیس:** حالت WAL برای همه‌ی دیتابیس‌ها (اشخاص، پلاک، گزارش‌ها)؛
  خواندن/نوشتن هم‌زمان تردها دیگر دیتابیس را قفل نمی‌کند.
- **بکاپ خودکار روزانه:** در هر اجرا فقط یک‌بار بکاپ گرفته می‌شود؛
  ۷ بکاپ آخر در پوشه‌ی `backups` نگه داشته می‌شود.
- **نگهبان حافظه:** ترد هر دوربین هر ۶۰۰ فریم حافظه‌ی بلااستفاده را آزاد می‌کند؛
  مناسب سیستم‌های ۴ گیگابایت رم در پخش طولانی‌مدت.
- موارد قبلی همچنان پابرجاست: تور امنیت سراسری (ثبت کرش بدون بستن برنامه)،
  اتصال مجدد خودکار RTSP، کول‌داون ۱۵ ثانیه‌ای پلاک.

## 2.0.34-beta
- ثبت پلاک و شخصِ در حال حرکت (ترک آگاه از حرکت، تأیید با ۲ خوانش)؛
  زمان‌بندی متناوب مدل‌های سنگین برای سبک‌تر شدن هر تیک.

## 2.0.33-beta
- حذف مسیرهای پیش‌فرض، ویرایش مسیر، دابل‌کلیک = حذف نقطه،
  حذف شبیه‌سازی مسیر، ترمیم رفرش پوشش زنده.
