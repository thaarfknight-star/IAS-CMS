# سیستم پلاک‌خوان IAS-CMS 🚗

صفحه‌ی مستقل «پلاک‌خوان» در هدر (مثل Face Library) با دو تب:
- **📝 تعریف پلاک‌ها** — تعریف حرفه‌ای پلاک (خودرو 🚗 / موتورسیکلت 🏍 / سایر) + انتخاب دوربین‌هایی که پلاک‌خوان روی آن‌ها فعال است
- **📋 گزارش عبور** — گزارش عبور پلاک‌های تعریف‌شده / تعریف‌نشده با تصویر، تاریخ شمسی و ساعت + فیلتر نوع پلاک (خودرو/موتورسیکلت)

> **پلاک موتورسیکلت:** فرم تعریف سه تب دارد — «🚗 پلاک خودرو» (۲ رقم + حرف + ۳ رقم + کد ایران)، «🏍 پلاک موتورسیکلت» (۳ رقم ردیف بالا + ۱ رقم و حرف ردیف پایین، مثلاً «۱۲۳ ۴ب») و «سایر پلاک‌ها». تطبیق فازی فقط بین پلاک‌های هم‌نوع انجام می‌شود تا خوانش یک موتور اشتباهی به پلاک خودرو نسبت داده نشود. دیتابیس‌های قدیمی خودکار مهاجرت می‌کنند (ستون `plate_type` اضافه و نوع پلاک‌های قبلی حدس زده می‌شود).

## فایل‌های جدید (کنار main.py کپی کنید)

| فایل | نقش |
|---|---|
| `plate_store.py` | دیتابیس SQLite پلاک‌ها و رویدادهای عبور (`plate_data/plates.db`)؛ نرمال‌سازی ارقام فارسی/عربی، تطبیق فازی (آستانه ۰٫۸۲)، کول‌داون ۴۵ ثانیه، تاریخ شمسی داخلی |
| `plate_detector.py` | تشخیص ناحیه‌ی پلاک با YOLOv8 + OCR چندموتوره (EasyOCR فارسی، بعد RapidOCR) + ردیاب چندفریمی (تأیید ۲ خوانش یکسان، کول‌داون) |
| `plate_library_dialog.py` | صفحه‌ی `PlateLibraryPage` با دو تب + فرم حرفه‌ای تعریف پلاک + دیالوگ جزئیات عبور |

## فایل‌های تغییرکرده (جایگزین کنید)

- `main.py` — دکمه‌ی «🚗 پلاک‌خوان» در هدر، صفحه‌ی پنجم، `on_plate_event` (ثبت عبور)، اتصال `plate_event` به همه‌ی خانه‌ها، `_on_camera_plate_toggle` (اعمال زنده‌ی تیک دوربین)
- `camera_stream.py` — سیگنال `plate_event_signal`، `set_plate_detection()`، تشخیص+OCR+ردیابی فقط برای دوربین فعال، رسم باکس (سبز=تعریف‌شده، نارنجی=ناشناس)
- `build.yml` — باندل شرطی `plate_detector.pt` و `easyocr`/`rapidocr_onnxruntime` (فقط اگر در محیط بیلد نصب باشند؛ نبودشان بیلد را نمی‌شکند)

## مدل تشخیص پلاک (اعتبارسنجی‌شده ✅)

`joker5914/yolov8n-license-plate` از HuggingFace — لینک مستقیم (تست HTTP 200 در 2026-09-13):
```
https://huggingface.co/joker5914/yolov8n-license-plate/resolve/main/best.pt
```
~۶ مگابایت، YOLOv8n، سازگار با ultralytics. برنامه در **اولین استفاده** (فقط وقتی پلاک‌خوانِ یک دوربین فعال شود) خودش آن را در `plate_data/models/` دانلود می‌کند؛ می‌توانید فایل را دستی هم همان‌جا یا کنار exe با نام `plate_detector.pt` بگذارید. با متغیر محیطی `IAS_PLATE_MODEL` / `IAS_PLATE_MODEL_URL` هم می‌شود مسیر/لینک دیگری داد.

## نصب (اختیاری — روی ویندوز)

```bat
pip install easyocr rapidocr-onnxruntime
```

> **نکته‌ی مهم (رفع مشکل «باکس می‌آید ولی متن خوانده نمی‌شود»):** نسخه‌ی exe که
> گیت‌هاب می‌سازد از این به بعد `easyocr` و `rapidocr-onnxruntime` را خودش نصب
> و داخل exe می‌گذارد (قدم `Install Dependencies` در `build.yml`)، پس نیازی به
> نصب دستی روی ویندوز نیست. در **اولین خوانش واقعی**، مدل‌های easyocr (حدود
> ۱۰۰ مگابایت، یک‌بار) دانلود می‌شوند؛ اینترنت لازم است.
> اگر موتور OCR در دسترس نباشد، برنامه دیگر باکس خالیِ بی‌صدا نمی‌گذارد:
> بالای صفحه‌ی پلاک‌خوان وضعیت موتور نمایش داده می‌شود و هنگام فعال‌سازی،
> پیام هشدار واضح می‌آید.
- **EasyOCR** (با مدل فارسی `fa`) موتور اصلی خوانش پلاک ایرانی است؛ سنگین است (~۱۰۰ مگابایت مدل) و فقط هنگام اولین خوانش واقعی لود می‌شود.
- **RapidOCR** سبک و کمکی است (برای پلاک ایرانی به‌تنهایی کافی نیست).
- اگر هیچ‌کدام نصب نباشند برنامه **بدون کرش** کار می‌کند؛ فقط خوانش متن پلاک انجام نمی‌شود و پیام روشن نمایش داده می‌شود.

## نکات مهم

- **۴GB RAM / بدون GPU**: همه‌ی مدل‌ها (پلاک، شخص، حریق) تنبل لود می‌شوند؛ پلاک‌خوان فقط روی دوربین‌هایی که تیک خورده‌اند اجرا می‌شود و در همان سمافور سراسری تشخیص (حداکثر ۲ زنجیره‌ی هم‌زمان) سهم می‌برد تا پخش زنده کند نشود.
- فعال‌بودن پلاک‌خوان هر دوربین در `cameras.json` با کلید `plate_detection` ذخیره می‌شود (از طریق `camera_store.update_camera`).
- تاریخ‌ها شمسی و داخلی‌اند (بدون وابستگی خارجی)؛ خروجی CSV گزارش هم هست.
- تعریف پلاک ناشناس مستقیم از روی ردیف گزارش («تعریف این پلاک») انجام می‌شود و رویداد به آن متصل می‌گردد.

## دستورهای push (در ریشه‌ی ریپو)

```bat
git add main.py camera_stream.py plate_store.py plate_detector.py plate_library_dialog.py .github\workflows\build.yml
git commit -m "Add license plate recognition (PlateLibraryPage + per-camera toggle + crossing report)"
git push
```

> اگر `plate_detector.pt` را دستی دانلود کرده‌اید و می‌خواهید در exe باندل شود، آن را هم `git add` کنید (اختیاری — وگرنه برنامه خودش دانلود می‌کند).
