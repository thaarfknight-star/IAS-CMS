# Minimal stub so the app can import without the real (heavy) dlib-based
# face_recognition package installed in this sandbox.
def face_locations(*a, **k): return []
def face_encodings(*a, **k): return []
def face_distance(*a, **k): return []
def compare_faces(*a, **k): return []
