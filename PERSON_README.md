# 👥 ردیابی اشخاص بین دوربین‌ها (با کمک چهره)

سیستمی که مسیر حرکت هر شخص را بین دوربین‌ها دنبال می‌کند: مثلاً کسی از
«ورودی اصلی» وارد می‌شود و به «اتاق فنی» می‌رود — هر دوربینی که او را
ببیند، ورود/خروجش را با روز و ساعت دقیق ثبت می‌کند و صفحه‌ی «ردیابی
اشخاص» مسیر کاملش را نشان می‌دهد.

**معماری نسخه‌ی ۲** (بازسازی بر اساس الگوی استاندارد جهانی
tracking-by-detection مثل ByteTrack/SORT):
- **تشخیص:** YOLOv8n (کلاس person) — بدون تغییر؛
- **ردیابی محلی:** فیلتر کالمن (سرعت ثابت) برای هر رد + تطبیق دومرحله‌ای
  حریصانه‌ی IoU با «باکس پیش‌بینی‌شده» (نه باکس فریم قبل)؛ IoU < ۰٫۲
  همیشه رد می‌شود؛ ردهای گمشده تا ۲۰ تیک نگه داشته می‌شوند و با بازگشت
  شخص، همان شناسه ادامه پیدا می‌کند (بدون ثبت تکراری)؛
- **توصیف‌گر ظاهری:** هیستوگرام HSV سه ناحیه‌ی بدن با وزن مرکزی (ضد نشت
  پس‌زمینه) + هیستوگرام جهت گرادیان (بافتِ نامتغیر نسبت به نور)؛ بردار
  نهایی L2-نرمال و فاصله‌ی بین دوربینی «کسینوسی» (معیار استاندارد ReID)؛
- **کمک چهره:** خروجی آماده‌ی FaceEngine همان دور تشخیص (هزینه‌ی صفر)؛
  چهره‌ی شناخته‌شده در بانک چهره‌ها = تطبیق قطعی حتی با لباس عوض‌شده؛
- **تشخیصی:** شمارنده‌های زنده در صفحه‌ی ردیابی + فایل
  `person_data/person_debug.log` تا معلوم باشد مسیر
  candidate→confirmed→store دقیقاً کجا می‌ایستد.

> **رفع ریشه‌ای باگ «هیچ گزارشی ثبت نمی‌شود» (نسخه‌ی ۲):** خروجی
> `PersonDetector.detect()` قالب `(top, right, bottom, left)` دارد ولی
> ردیاب قدیمی آن را `(x1, y1, x2, y2)` می‌خواند؛ کراپ همیشه نامعتبر
> می‌شد و رویداد `confirmed` هرگز صادر نمی‌شد. حالا باکس‌ها در ورودی
> ردیاب نرمال و اعتبارسنجی می‌شوند و اگر توصیف‌گر لحظه‌ی تأیید ساخته
> نشود، تأیید «بی‌صدا» انجام نمی‌شود بلکه در تیک بعد دوباره تلاش می‌شود.

## فایل‌ها

| فایل | نقش |
|---|---|
| `person_reid.py` | **بازنویسی کامل** — نرمال‌سازی باکس، کالمن، تطبیق دومرحله‌ای IoU، توصیف‌گر v2 (کسینوسی)، تطبیق سراسری، شمارنده‌های تشخیصی |
| `person_store.py` | **تغییر کوچک** — پیش‌فرض آستانه‌ی تطبیق ۰٫۴۵ (فاصله‌ی کسینوسی) |
| `person_track_dialog.py` | **تغییر** — بخش «🔍 وضعیت زنده‌ی ردیابی (تشخیصی)» + راهنمای خواندن شمارنده‌ها |
| `camera_stream.py` | **تغییر** — `get_person_diag()` و لاگ دوره‌ای `person_debug.log` |
| `main.py` | بدون تغییر (قرارداد رویدادها عوض نشده) |

## روش کار (خلاصه)

1. در صفحه‌ی «ردیابی اشخاص» تب «اشخاص»، تیک دوربین‌های موردنظر را بزن
   (مثلاً ورودی اصلی و اتاق فنی). تنظیم در `cameras.json` ذخیره و بلافاصله
   روی دوربین‌های باز هم اعمال می‌شود.
2. هر دوربینِ فعال، اشخاص داخل کادرش را با ردیاب محلی (IoU چندفریمی)
   دنبال می‌کند؛ بعد از چند فریم پیاپی، «تأیید» می‌شود و یک توصیف‌گر
   ظاهری از او ساخته می‌شود: رنگ لباس، رنگ شلوار، رنگ و بلندی مو.
3. توصیف‌گر با اشخاصی که در چند دقیقه‌ی اخیر در دوربین‌های دیگر دیده
   شده‌اند مقایسه می‌شود:
   - شبیه بود → همان شخص قبلی (کد `P-0001` و...)؛ حضور تازه‌ای در
     «مسیر» او ثبت می‌شود و روی تصویر همان کد نمایش داده می‌شود.
   - شبیه نبود → شخص تازه‌ای با کد جدید ثبت می‌شود.
4. وقتی شخص از کادر دوربین خارج شد (یا دوربین بسته شد)، «حضور» با ساعت
   خروج بسته می‌شود. در تب «گزارش مسیر حرکت»، خط‌زمانی کامل هر شخص را
   با تاریخ شمسی و ساعت دقیق ورود/خروج هر دوربین (اتاق) می‌بینی + خروجی CSV.

## تنظیمات (تب «اشخاص» → «تنظیمات تطبیق ظاهری»)

| تنظیم | پیش‌فرض | اثر |
|---|---|---|
| آستانه‌ی تطبیق | ۰٫۴۵ | فاصله‌ی کسینوسی بردار نرمال‌شده (۰ = یکسان). کمتر = سخت‌گیرانه‌تر؛ بیشتر = بخشنده‌تر (خطر یکی شدن دو شخص شبیه‌به‌هم) |
| پنجره‌ی اتصال (دقیقه) | ۱۰ | اگر شخصی بعد از این مدت در دوربین دیگری دیده شود، شخص تازه ثبت می‌شود |
| فریم تأیید | ۳ | شخص باید این‌تعداد فریم پیاپی دیده شود تا ردش ثبت شود |

## محدودیت‌های صادقانه (مهم)

- **این بازشناسی هویتی نیست.** سیستم چهره را نمی‌بیند و فقط «ظاهر»
  (رنگ لباس/شلوار/مو) را مقایسه می‌کند. دو نفر با لباس خیلی شبیه ممکن
  است یکی حساب شوند؛ اگر کسی کاپشنش را عوض کند، ردش می‌شکافد و شخص
  جدیدی ثبت می‌شود.
- دقت به زاویه‌ی دوربین و نور بستگی دارد؛ نمای تمام‌قد بهترین نتیجه را می‌دهد.
- مدل سنگین جدیدی اضافه نشده؛ روی همان سخت‌افزار ۴GB بدون GPU کار می‌کند.

## پوش

```bat
git add person_reid.py person_store.py person_track_dialog.py camera_stream.py PERSON_README.md
git commit -m "Rebuild person tracking v2: ByteTrack-style Kalman+IoU, box-format fix, cosine appearance embedding, live diagnostics"
git push
```

بعد از بیلد exe، در صفحه‌ی «ردیابی اشخاص» تیک دوربین‌ها را بزن؛ با
عبور یک نفر جلوی دو دوربین، باید در تب «اشخاص» یک ردیف با کد `P-0001`
و در تب «گزارش مسیر حرکت» دو حضور (ورود/خروج هر دوربین) ببینی.
