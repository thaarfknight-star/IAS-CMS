PyQt6>=6.6.0
# نکته‌ی تاریخی: قبلاً این خط برای رفع باگ حذف HOGDescriptor در OpenCV 5.0
# به زیر نسخه‌ی ۵ سقف زده شده بود؛ چون شمارش افراد دیگر اصلاً به
# HOGDescriptor وابسته نیست (رجوع کنید به camera_stream.py - از تعداد
# چهره‌های شناسایی‌شده استفاده می‌شود، نه یک تشخیص‌دهنده‌ی جدا)، این سقف
# لازم نیست و برداشته شده.
opencv-python>=4.8.0
face-recognition>=1.3.0
numpy>=1.24.0
requests>=2.31.0

# اختیاری: برای کشف دقیق‌تر کانال‌های NVR از طریق پروتکل استاندارد ONVIF
# (در صورت نصب‌نبودن، برنامه به‌صورت خودکار از روش تست الگوهای RTSP برندهای
# رایج - Dahua/Hikvision/Sunell/... - استفاده می‌کند، پس این بسته اجباری نیست)
onvif-zeep>=0.2.12

# اختیاری ولی به‌شدت توصیه‌شده: تشخیص «شخص» (کل بدن، در هر حالتی - ایستاده،
# نشسته، نیم‌خیز، خم‌شده، پشت به دوربین و ...) برای شمارش دقیق افراد،
# مستقل از دیده‌شدن چهره (رجوع کنید به person_detector.py). اگر این بسته
# نصب نباشد، برنامه crash نمی‌کند - فقط شمارش به روش قدیمی‌تر (بر پایه‌ی
# تعداد چهره‌های شناسایی‌شده) برمی‌گردد.
#
# اگر خروجی‌تان همان exe پرتابل ساخته‌شده با GitHub Actions است (رجوع کنید
# به .github/workflows/build.yml)، این خط و خود وزن مدل (yolov8n.pt) کاملاً
# روی سرور گیت‌هاب نصب/دانلود و مستقیماً داخل همان یک exe بسته‌بندی
# می‌شوند - روی سیستم شما هیچ‌چیز نصب یا دانلود نمی‌شود، حتی اتصال اینترنت
# هم لازم نیست. این خط فقط برای کسی مهم است که بخواهد پروژه را از سورس
# (`python main.py`) روی سیستم خودش اجرا کند؛ در آن حالت اولین اجرا وزن
# مدل (~۶ مگابایت) را یک‌بار خودکار دانلود می‌کند.
ultralytics>=8.1.0

# اختیاری: برای دیالوگ «پنل وب NVR» (نمایش پنل وب واقعی یک NVR داخل خود
# برنامه، مخصوص NVRهایی که سرویس RTSP‌شان به‌دلیل باگ فریمور با هیچ کلاینت
# استانداردی - این برنامه، VLC و ... - کار نمی‌کند؛ رجوع کنید به
# nvr_webview_dialog.py). اگر نصب نباشد، فقط همین یک گزینه از منو غیرفعال
# می‌شود و بقیه‌ی برنامه طبق معمول کار می‌کند.
PyQt6-WebEngine>=6.6.0

# نکته: pyinstaller دیگر برای ساخت خروجی استفاده نمی‌شود (رجوع کنید به
# .github/workflows/build.yml) - به‌جای آن Nuitka به‌کار رفته چون هم فایل
# پرتابل تکی (--onefile) می‌دهد و هم برخلاف PyInstaller، کد را واقعاً به
# باینری ماشین کامپایل می‌کند (نه بسته‌بندی بایت‌کد قابل استخراج). Nuitka
# به‌صورت جدا و فقط داخل CI نصب می‌شود، نیازی به بودنش برای اجرای عادی
# برنامه از سورس نیست.
