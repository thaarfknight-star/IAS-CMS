PyQt6>=6.6.0
# نکته: تشخیص شخص (person_detector.py) از HOGDescriptor استفاده می‌کند که
# در OpenCV 5.0 حذف شده بود و بعداً برگردانده شد؛ اگر با نسخه‌ی خاصی از
# OpenCV به این باگ برخوردید (خطای نبودن HOGDescriptor)، نسخه را به زیر ۵
# سقف بزنید (مثلاً opencv-python<5) تا مشکل رفع شود.
opencv-python>=4.8.0
face-recognition>=1.3.0
numpy>=1.24.0
requests>=2.31.0

# اختیاری: برای کشف دقیق‌تر کانال‌های NVR از طریق پروتکل استاندارد ONVIF
# (در صورت نصب‌نبودن، برنامه به‌صورت خودکار از روش تست الگوهای RTSP برندهای
# رایج - Dahua/Hikvision/Sunell/... - استفاده می‌کند، پس این بسته اجباری نیست)
onvif-zeep>=0.2.12

# نکته: تشخیص «شخص» (کل بدن، در هر حالتی - ایستاده، نشسته، نیم‌خیز، خم‌شده،
# پشت به دوربین و ...) دیگر روی هیچ مدل یادگیری‌ماشینی خارجی (نه ultralytics،
# نه torch) سوار نیست. person_detector.py این کار را فقط با ابزارهای خودِ
# opencv-python (که همین بالا در همین فایل الزامی است) انجام می‌دهد -
# تفریق پس‌زمینه (MOG2) + HOGDescriptor + Haar Cascade های کل‌بدن/نیم‌تنه.
# یعنی هیچ بسته‌ی اضافه‌ای لازم نیست و هیچ فایل وزن مدلی دانلود نمی‌شود.

# اختیاری: برای دیالوگ «پنل وب NVR» (نمایش پنل وب واقعی یک NVR داخل خود
# برنامه، مخصوص NVRهایی که سرویس RTSP‌شان به‌دلیل باگ فریمور با هیچ کلاینت
# استانداردی - این برنامه، VLC و ... - کار نمی‌کند؛ رجوع کنید به
# nvr_webview_dialog.py). اگر نصب نباشد، فقط همین یک گزینه از منو غیرفعال
# می‌شود و بقیه‌ی برنامه طبق معمول کار می‌کند.
PyQt6-WebEngine>=6.6.0

# نکته: pyinstaller دیگر برای ساخت خروجی استفاده نمی‌شود (رجوع کنید به
# .github/workflows/build.yml) - به‌جای آن Nuitka به‌کار رفته چون هم فایل
# پرتابل تکی (--onefile) می‌دهد و هم برخلاف PyInstaller، کد را واقعاً به
# باینری ماشین کامپایل می‌کند (نه بسته‌بندی بایت‌کد قابل استخراج). Nuitka
# به‌صورت جدا و فقط داخل CI نصب می‌شود، نیازی به بودنش برای اجرای عادی
# برنامه از سورس نیست.
